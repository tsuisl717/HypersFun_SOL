import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ message: 'LiFi bridge removed — migrating to Solana' }, { status: 404 });
}

export async function POST() {
  return NextResponse.json({ message: 'LiFi bridge removed — migrating to Solana' }, { status: 404 });
}
